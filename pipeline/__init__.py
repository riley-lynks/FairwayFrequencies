# pipeline/__init__.py
# This file marks the pipeline/ folder as a Python "package" —
# it tells Python "this folder contains importable modules".
# Without this file, the imports in fairway.py would fail.
# It can be empty. We keep it empty on purpose — no code needed here.
